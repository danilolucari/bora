<?xml version="1.0" encoding="utf-8"?>
<!--
  Libera HTTP em texto claro APENAS para os emuladores do Firebase.

  Do Android 9 (API 28) em diante o texto claro é bloqueado por padrão, e os
  emuladores do Firebase falam HTTP puro. Sem esta exceção o app morre com
  "Cleartext HTTP traffic to 10.0.2.2 not permitted" ao tentar autenticar.

  `10.0.2.2` é como o emulador de Android enxerga o localhost da máquina —
  é o mesmo host que `EmulatorConfig.host` devolve quando roda em Android.

  Este arquivo vive em `src/debug/`, então **não entra no APK de release**:
  a política de rede de produção continua a padrão do Android, sem exceção
  nenhuma. É a mesma fronteira que `connectEmulators` já respeita em Dart,
  onde `kReleaseMode` faz a função sair cedo (AD-004, emulator-first).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
